# Test override: misc.yml for tests (Redis → modules/redis.yml).

paths:
  feature-config-directory: modules

join:
  grace-period: 5

portal:
  enabled: false

bees:
  enabled: false

xaction:
  server-name: test-server
  enabled: false

sync:
  enabled: false

date-format: "dd.MM.yyyy"
timezone: "Europe/Moscow"

worlds: {}
world-names: {}
respawn-intercept-worlds: []
