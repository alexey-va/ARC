# Testcontainers configuration for Colima
# Set DOCKER_HOST environment variable to use Colima:
# export DOCKER_HOST=unix://$HOME/.colima/default/docker.sock
# Or it will auto-detect from companion object init block

